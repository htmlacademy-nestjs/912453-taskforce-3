import { modelsReviewsModels } from './models-reviews-models';

describe('modelsReviewsModels', () => {
  it('should work', () => {
    expect(modelsReviewsModels()).toEqual('models-reviews-models');
  });
});
